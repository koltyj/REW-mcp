---
phase: 08-workflow-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/index.ts
  - src/prompts/index.ts
  - src/prompts/calibration-full.ts
  - src/prompts/gain-staging.ts
  - src/prompts/measurement-workflow.ts
  - src/prompts/optimization-workflow.ts
autonomous: true

must_haves:
  truths:
    - "Full calibration workflow prompt is available via rew_calibration_full"
    - "Gain staging prompt is available standalone via rew_gain_staging"
    - "Measurement workflow prompt requires session_id via rew_measurement_workflow"
    - "Optimization workflow prompt requires session_id via rew_optimization_workflow"
    - "Prompts describe goals and context, not tool sequences"
  artifacts:
    - path: "src/prompts/index.ts"
      provides: "Prompt registration with MCP server"
      exports: ["registerPrompts"]
    - path: "src/prompts/calibration-full.ts"
      provides: "Master calibration workflow prompt"
      exports: ["CALIBRATION_FULL_PROMPT", "getCalibrationFullMessages"]
    - path: "src/prompts/gain-staging.ts"
      provides: "Standalone gain staging prompt"
      exports: ["GAIN_STAGING_PROMPT", "getGainStagingMessages"]
    - path: "src/prompts/measurement-workflow.ts"
      provides: "Session-aware measurement prompt"
      exports: ["MEASUREMENT_WORKFLOW_PROMPT", "getMeasurementWorkflowMessages"]
    - path: "src/prompts/optimization-workflow.ts"
      provides: "Session-aware optimization prompt"
      exports: ["OPTIMIZATION_WORKFLOW_PROMPT", "getOptimizationWorkflowMessages"]
  key_links:
    - from: "src/index.ts"
      to: "src/prompts/index.ts"
      via: "registerPrompts(server)"
      pattern: "registerPrompts\\(server\\)"
    - from: "src/prompts/measurement-workflow.ts"
      to: "src/session/session-state.ts"
      via: "getSession for embedded resource"
      pattern: "import.*getSession.*from.*session"
---

<objective>
Implement MCP Prompts for calibration workflows - master prompt for full calibration, plus standalone prompts for gain staging, measurement, and optimization sub-workflows.

Purpose: Provide goal-oriented workflow templates that Claude can follow for studio calibration, with appropriate session awareness and embedded resources where needed.

Output: Four prompts registered with MCP server: rew_calibration_full, rew_gain_staging, rew_measurement_workflow, rew_optimization_workflow.
</objective>

<execution_context>
@/Users/koltonjacobs/.claude-gsd/get-shit-done/workflows/execute-plan.md
@/Users/koltonjacobs/.claude-gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-workflow-orchestration/08-RESEARCH.md
@.planning/phases/08-workflow-orchestration/08-01-SUMMARY.md (when available)
@src/index.ts
@src/session/session-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompt infrastructure and master calibration prompt</name>
  <files>
    src/index.ts
    src/prompts/index.ts
    src/prompts/calibration-full.ts
  </files>
  <action>
Update src/index.ts to import and call registerPrompts(server) after registerResources.

Create src/prompts/calibration-full.ts:
1. Export CALIBRATION_FULL_PROMPT definition:
   ```typescript
   {
     name: 'rew_calibration_full',
     title: 'Full Calibration Workflow',
     description: 'Complete studio calibration: gain staging, L/R/Sub measurement, room analysis, optimization guidance',
     arguments: [
       { name: 'target_spl_db', description: 'Target SPL for monitor calibration (default: 85 dB)', required: false },
       { name: 'room_dimensions', description: 'Room dimensions in feet "LxWxH" (e.g., "12x10x8")', required: false }
     ]
   }
   ```

2. Export getCalibrationFullMessages(args) function that returns goal-oriented messages:
   - Parse target_spl_db (default 85) and room_dimensions from args
   - Return messages array with single user message describing:
     - OBJECTIVES: gain staging, measurement, analysis, optimization
     - WORKFLOW: when to pause (physical actions, decisions), autonomous between checkpoints
     - SESSION MANAGEMENT: use rew.api_measurement_session for state
     - RESOURCES: use session:// and recommendations:// for context
     - SCIENTIFIC METHOD: one recommendation at a time, validate each
   - Message should NOT prescribe exact tool sequence (that's Claude's job)

Create src/prompts/index.ts:
1. Import ListPromptsRequestSchema, GetPromptRequestSchema from @modelcontextprotocol/sdk/types.js
2. Import prompt definitions and message generators from each prompt module
3. Export registerPrompts(server) that:
   - Sets ListPromptsRequestSchema handler returning all prompt definitions
   - Sets GetPromptRequestSchema handler that:
     - Extracts name and arguments from request.params
     - Calls appropriate message generator
     - Returns { description, messages } response
     - Throws Error for unknown prompt name
  </action>
  <verify>
    TypeScript compiles: `npm run build`
  </verify>
  <done>
    Prompt infrastructure created. Master calibration prompt returns goal-oriented messages with target_spl_db and room_dimensions arguments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create standalone and session-aware sub-prompts</name>
  <files>
    src/prompts/gain-staging.ts
    src/prompts/measurement-workflow.ts
    src/prompts/optimization-workflow.ts
    src/prompts/index.ts
  </files>
  <action>
Create src/prompts/gain-staging.ts:
1. Export GAIN_STAGING_PROMPT definition:
   - name: 'rew_gain_staging'
   - title: 'Gain Staging Only'
   - description: 'Calibrate monitor levels to target SPL using pink noise and SPL meter'
   - arguments: [{ name: 'target_spl_db', description: 'Target SPL (default: 85 dB)', required: false }]
2. Export getGainStagingMessages(args) returning goal-oriented message:
   - OBJECTIVE: Set monitor output to target SPL
   - CONTEXT: Use rew.api_calibrate_spl (start/check/stop pattern)
   - WORKFLOW: Play pink noise, measure SPL, guide user to adjust volume
   - PAUSE: When user needs to physically adjust monitor volume
   - Note: This is standalone (no session required)

Create src/prompts/measurement-workflow.ts:
1. Import getSession from ../session/index.js
2. Export MEASUREMENT_WORKFLOW_PROMPT definition:
   - name: 'rew_measurement_workflow'
   - title: 'Measurement Sequence'
   - description: 'Guided L/R/Sub measurement with session state tracking'
   - arguments: [{ name: 'session_id', required: true, description: 'Active session ID' }]
3. Export getMeasurementWorkflowMessages(args) returning:
   - Fetch session state via getSession(args.session_id) - throw if missing/invalid
   - Include embedded resource with current session state summary
   - Goal-oriented message: complete L/R/Sub sequence, name measurements appropriately
   - WORKFLOW: Use rew.api_measurement_session measure action
   - PAUSE: Before each measurement to confirm speaker selection/positioning

Create src/prompts/optimization-workflow.ts:
1. Import getSession from ../session/index.js
2. Export OPTIMIZATION_WORKFLOW_PROMPT definition:
   - name: 'rew_optimization_workflow'
   - title: 'Room Optimization'
   - description: 'Iterative placement optimization with measurement validation'
   - arguments: [{ name: 'session_id', required: true, description: 'Session with completed measurements' }]
3. Export getOptimizationWorkflowMessages(args) returning:
   - Fetch and validate session (must have measurements)
   - Include embedded resource with session state
   - Goal-oriented message: analyze room, get recommendations, validate adjustments
   - WORKFLOW: Use rew.analyze_room, rew.optimize_room (get_recommendation, validate_adjustment, check_progress)
   - SCIENTIFIC METHOD: One recommendation at a time, measure after each change
   - PAUSE: When user needs to physically move speakers/listener

Update src/prompts/index.ts:
- Import all prompt definitions and message generators
- Add to ListPromptsRequestSchema handler
- Add cases to GetPromptRequestSchema handler
  </action>
  <verify>
    TypeScript compiles: `npm run build`
  </verify>
  <done>
    Four prompts available: rew_calibration_full (master), rew_gain_staging (standalone), rew_measurement_workflow (session-aware), rew_optimization_workflow (session-aware).
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify prompt integration and message quality</name>
  <files>
    src/prompts/index.ts
  </files>
  <action>
Final verification and quality pass on prompt messages:

1. Verify all prompts are registered:
   - ListPromptsRequestSchema returns all 4 prompts
   - Each prompt has name, title, description, arguments array

2. Verify prompt messages follow CONTEXT.md decisions:
   - Goal-oriented (describe objectives and context, NOT tool sequences)
   - Checkpoints defined (physical actions, decisions)
   - Session awareness (measurement and optimization require session_id)
   - Error handling guidance (retry with troubleshooting)

3. Verify embedded resources in session-aware prompts:
   - measurement-workflow embeds session state
   - optimization-workflow embeds session state
   - Format: { role: 'assistant', content: { type: 'resource', resource: { uri, mimeType, text } } }

4. Add any missing elements to meet requirements:
   - WKFL-01: Calibration session prompt - rew_calibration_full
   - WKFL-02: Gain staging prompt - rew_gain_staging
   - WKFL-03: Level calibration prompt - covered by rew_gain_staging
   - WKFL-04: Measurement workflow prompt - rew_measurement_workflow

Build and verify exports.
  </action>
  <verify>
    TypeScript compiles: `npm run build`
    Prompt exports: `node -e "import('./dist/prompts/index.js').then(m => console.log('registerPrompts:', typeof m.registerPrompts))"`
    Server starts: `timeout 2 node dist/index.js 2>&1 || true`
  </verify>
  <done>
    All prompts registered. Messages are goal-oriented. Session-aware prompts embed resources. WKFL-01 through WKFL-04 satisfied.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `npm run build`
2. Server starts: `timeout 2 node dist/index.js 2>&1 || true`
3. Prompt module exports: `node -e "import('./dist/prompts/index.js').then(m => console.log(Object.keys(m)))"`
4. All prompt files exist and export correctly
</verification>

<success_criteria>
- Server declares prompts capability with listChanged: true
- Four prompts registered: rew_calibration_full, rew_gain_staging, rew_measurement_workflow, rew_optimization_workflow
- Prompts describe goals and context (not prescriptive tool sequences)
- Session-aware prompts embed session state resource
- Prompts define appropriate checkpoint moments (physical actions, decisions)
- WKFL-01, WKFL-02, WKFL-03, WKFL-04 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-workflow-orchestration/08-02-SUMMARY.md`
</output>

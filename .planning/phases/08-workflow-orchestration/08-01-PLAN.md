---
phase: 08-workflow-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/resources/index.ts
  - src/resources/session-resource.ts
  - src/resources/measurement-resource.ts
  - src/resources/recommendations-resource.ts
autonomous: true

must_haves:
  truths:
    - "Session state is accessible via session://{id} URI"
    - "Measurement data is accessible via measurement://{id} URI"
    - "Recommendations are accessible via recommendations://{session_id} URI"
    - "Resource list updates when sessions are created/ended"
  artifacts:
    - path: "src/resources/index.ts"
      provides: "Resource registration with MCP server"
      exports: ["registerResources"]
    - path: "src/resources/session-resource.ts"
      provides: "Session state read handler"
      exports: ["readSessionResource"]
    - path: "src/resources/measurement-resource.ts"
      provides: "Measurement data read handler"
      exports: ["readMeasurementResource"]
    - path: "src/resources/recommendations-resource.ts"
      provides: "Recommendations read handler"
      exports: ["readRecommendationsResource"]
  key_links:
    - from: "src/index.ts"
      to: "src/resources/index.ts"
      via: "registerResources(server)"
      pattern: "registerResources\\(server\\)"
    - from: "src/resources/session-resource.ts"
      to: "src/session/session-state.ts"
      via: "getSession import"
      pattern: "import.*getSession.*from.*session"
---

<objective>
Add MCP Resources capability to the server and implement resource handlers for session state, measurement data, and recommendations.

Purpose: Enable Claude to read session state and measurement data through MCP Resources, providing read-only access to workflow state without modifying it through tools.

Output: Working MCP Resources with session://, measurement://, and recommendations:// URI schemes.
</objective>

<execution_context>
@/Users/koltonjacobs/.claude-gsd/get-shit-done/workflows/execute-plan.md
@/Users/koltonjacobs/.claude-gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-workflow-orchestration/08-RESEARCH.md
@src/index.ts
@src/session/session-state.ts
@src/store/measurement.ts
@src/optimization/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resources capability and create resource infrastructure</name>
  <files>
    src/index.ts
    src/resources/index.ts
  </files>
  <action>
Update src/index.ts to:
1. Add resources capability to server initialization:
   ```typescript
   capabilities: {
     tools: { listChanged: true },
     prompts: { listChanged: true },
     resources: { subscribe: true, listChanged: true },
     logging: {}
   }
   ```
2. Import and call registerResources(server) after registerTools

Create src/resources/index.ts that:
1. Imports ListResourcesRequestSchema, ReadResourceRequestSchema, ListResourceTemplatesRequestSchema from @modelcontextprotocol/sdk/types.js
2. Imports listActiveSessions from ../session/index.js
3. Implements registerResources(server) function that:
   - Sets ListResourcesRequestSchema handler: Returns dynamic list of active session resources
   - Sets ListResourceTemplatesRequestSchema handler: Returns URI templates for session://{session_id}, measurement://{measurement_id}, recommendations://{session_id}, history://{session_id}
   - Sets ReadResourceRequestSchema handler: Parses URI scheme and delegates to appropriate resource handler

URI scheme parsing in ReadResourceRequestSchema:
```typescript
const match = uri.match(/^(\w+):\/\/(.+)$/);
if (!match) throw new Error(`Invalid resource URI: ${uri}`);
const [, scheme, path] = match;
```

Delegate to readSessionResource, readMeasurementResource, readRecommendationsResource based on scheme. For unknown scheme, throw Error with message "Unknown resource scheme: {scheme}".
  </action>
  <verify>
    TypeScript compiles: `npm run build`
  </verify>
  <done>
    Server declares resources capability. Resource handlers registered. URI parsing delegates to specific resource modules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement session, measurement, and recommendations resource handlers</name>
  <files>
    src/resources/session-resource.ts
    src/resources/measurement-resource.ts
    src/resources/recommendations-resource.ts
  </files>
  <action>
Create src/resources/session-resource.ts:
1. Import getSession, listActiveSessions from ../session/index.js
2. Export readSessionResource(sessionId: string) that:
   - Calls getSession(sessionId) - throws if not found with code -32002
   - Returns session state with measurement summaries (not full FR data)
   - Format: { session_id, created_at, sequence_step, measurements: [{ uuid, name, channel, timestamp }], target_spl, notes }
3. Export listSessionResources() for dynamic resource list:
   - Returns array of { uri: `session://${id}`, name, title, description, mimeType: 'application/json' }

Create src/resources/measurement-resource.ts:
1. Import measurementStore from ../store/measurement.js
2. Export readMeasurementResource(measurementId: string) that:
   - Gets measurement from store, throws -32002 error if not found
   - Returns full measurement data including frequency_response arrays
   - Format: { id, metadata, frequency_response: { frequencies_hz, spl_db }, timestamp }
3. Export listMeasurementResources() for dynamic resource list

Create src/resources/recommendations-resource.ts:
1. Import getSession from ../session/index.js
2. Export readRecommendationsResource(sessionId: string) that:
   - Gets session, throws -32002 error if not found
   - Returns recommendations placeholder: { session_id, current_recommendation: null, tried_recommendations: [], validation_status: null }
   - Note: Full recommendation tracking would require extending session state (out of scope for this plan)

Error handling pattern for -32002 (resource not found):
```typescript
if (!found) {
  const error = new Error(`{Resource type} not found: ${id}`);
  (error as any).code = -32002;
  throw error;
}
```
  </action>
  <verify>
    TypeScript compiles: `npm run build`
  </verify>
  <done>
    Session resource returns session state. Measurement resource returns full FR data. Recommendations resource returns placeholder structure. All throw -32002 for not found.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add history resource and verify integration</name>
  <files>
    src/resources/index.ts
    src/resources/history-resource.ts
  </files>
  <action>
Create src/resources/history-resource.ts:
1. Import getSession from ../session/index.js
2. Import measurementStore from ../store/measurement.js
3. Export readHistoryResource(sessionId: string) that:
   - Gets session, throws -32002 if not found
   - Maps session.measurements to measurement summaries with IDs and timestamps
   - For each measurement with uuid, fetch summary from measurementStore if available
   - Returns: { session_id, measurements: [{ measurement_id, channel, timestamp, summary: { variance_40_200hz?, peak_spl_db?, frequency_range? } }] }

Update src/resources/index.ts:
1. Add history:// scheme handling in ReadResourceRequestSchema handler
2. Import readHistoryResource
3. Delegate history:// URIs to readHistoryResource

Verify the complete integration:
- ListResourcesRequestSchema returns active sessions
- ListResourceTemplatesRequestSchema returns all 4 templates
- ReadResourceRequestSchema handles all 4 schemes
  </action>
  <verify>
    TypeScript compiles: `npm run build`
    Exports verify: `node -e "import('./dist/resources/index.js').then(m => console.log('registerResources:', typeof m.registerResources))"`
  </verify>
  <done>
    All 4 resource types implemented (session, measurement, recommendations, history). Server integrates resources. Resources satisfy WKFL-05, WKFL-06, WKFL-07.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `npm run build`
2. Server starts without errors: `timeout 2 node dist/index.js 2>&1 || true` (should see "REW MCP Server running")
3. Resource module exports: `node -e "import('./dist/resources/index.js').then(m => console.log(Object.keys(m)))"`
</verification>

<success_criteria>
- Server declares resources capability with subscribe: true and listChanged: true
- Four resource URI schemes work: session://, measurement://, recommendations://, history://
- Resource templates available for dynamic URIs
- All resource handlers throw -32002 for not found
- WKFL-05 (session state), WKFL-06 (measurement history), WKFL-07 (recommendations) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-workflow-orchestration/08-01-SUMMARY.md`
</output>

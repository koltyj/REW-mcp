---
phase: 03-calibration-setup-tools
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - src/api/rew-client.test.ts
  - src/tools/api-check-levels.test.ts
  - src/tools/api-calibrate-spl.test.ts
autonomous: true

must_haves:
  truths:
    - "REWApiClient input level methods have unit test coverage"
    - "api-check-levels tool has unit tests for all zones"
    - "api-calibrate-spl tool has unit tests for all actions"
    - "Test coverage maintains Phase 2 thresholds (80% API, 70% tools)"
  artifacts:
    - path: "src/api/rew-client.test.ts"
      provides: "Input level monitoring method tests"
      contains: "startInputLevelMonitoring"
    - path: "src/tools/api-check-levels.test.ts"
      provides: "Check levels tool tests"
      contains: "determineLevelZone"
    - path: "src/tools/api-calibrate-spl.test.ts"
      provides: "SPL calibration tool tests"
      contains: "api_calibrate_spl"
  key_links:
    - from: "src/tools/api-check-levels.test.ts"
      to: "src/tools/api-check-levels.ts"
      via: "import executeApiCheckLevels"
      pattern: "executeApiCheckLevels"
    - from: "src/tools/api-calibrate-spl.test.ts"
      to: "src/tools/api-calibrate-spl.ts"
      via: "import executeApiCalibrateSPL"
      pattern: "executeApiCalibrateSPL"
---

<objective>
Add unit tests for Phase 3 calibration tools

Purpose: Maintain test coverage and verify tool behavior across all scenarios
Output: Comprehensive test suites for new API methods and MCP tools
</objective>

<execution_context>
@/Users/koltonjacobs/.claude-gsd/get-shit-done/workflows/execute-plan.md
@/Users/koltonjacobs/.claude-gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calibration-setup-tools/03-RESEARCH.md

Reference for test patterns:
@src/api/rew-client.test.ts (API client test patterns with MSW)
@src/tools/api-generator.test.ts (tool test patterns with vi.mock)
@src/tools/api-spl-meter.test.ts (SPL meter test patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add input level monitoring tests to rew-client.test.ts</name>
  <files>src/api/rew-client.test.ts</files>
  <action>
Add a new describe block for input level monitoring methods. Use MSW handlers following existing patterns.

Add MSW handlers:
```typescript
http.get('http://127.0.0.1:4735/input-levels/commands', () => {
  return HttpResponse.json(['start', 'stop']);
}),
http.post('http://127.0.0.1:4735/input-levels/command', async ({ request }) => {
  const body = await request.json() as { command: string };
  return HttpResponse.json({ success: true });
}),
http.get('http://127.0.0.1:4735/input-levels/units', () => {
  return HttpResponse.json(['dBFS', 'dBV', 'dBu']);
}),
http.get('http://127.0.0.1:4735/input-levels/last-levels', () => {
  return HttpResponse.json({
    unit: 'dBFS',
    rms: [-15.2, -14.8],
    peak: [-8.5, -9.1],
    timeSpanSeconds: 0.5
  });
})
```

Test cases:
1. `getInputLevelCommands returns command array` - Verify returns ['start', 'stop']
2. `startInputLevelMonitoring returns true on success` - Verify returns true for 200
3. `stopInputLevelMonitoring returns true on success` - Verify returns true for 200
4. `getInputLevelUnits returns unit array` - Verify returns ['dBFS', 'dBV', 'dBu']
5. `getInputLevels returns normalized levels` - Verify transforms rms->rms_levels, peak->peak_levels
6. `getInputLevels returns null when monitoring not started` - Use handler that returns 404
7. `getInputLevels passes unit parameter` - Verify query string includes unit
  </action>
  <verify>`npm test -- --testPathPattern="rew-client" --run` passes all new tests</verify>
  <done>REWApiClient input level methods have full test coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create api-check-levels.test.ts</name>
  <files>src/tools/api-check-levels.test.ts</files>
  <action>
Create new test file following patterns from api-generator.test.ts and api-spl-meter.test.ts.

Use vi.mock for getActiveApiClient:
```typescript
vi.mock('./api-connect.js', () => ({
  getActiveApiClient: vi.fn()
}));
```

Test cases for zone determination (export determineLevelZone for testing or test via executeApiCheckLevels):
1. `returns CLIPPING when peak > -3 dBFS` - peak=-2, rms=-15 -> CLIPPING
2. `returns HOT when rms > -10 dBFS` - peak=-6, rms=-8 -> HOT
3. `returns OPTIMAL when rms between -20 and -10 dBFS` - peak=-6, rms=-15 -> OPTIMAL
4. `returns LOW when rms between -40 and -20 dBFS` - peak=-25, rms=-30 -> LOW
5. `returns VERY_LOW when rms < -40 dBFS` - peak=-45, rms=-50 -> VERY_LOW

Test cases for executeApiCheckLevels:
1. `returns connection error when not connected` - getActiveApiClient returns null
2. `returns zone and feedback for optimal level` - Mock optimal levels, verify zone='OPTIMAL', should_block_measurement=false
3. `returns should_block_measurement true for CLIPPING` - Mock clipping levels, verify blocking
4. `returns should_block_measurement true for VERY_LOW` - Mock very low levels, verify blocking
5. `warns on L/R channel mismatch > 3 dB` - Mock [-12, -18] RMS, verify warning in feedback
6. `averages RMS across channels` - Mock [(-10 + -14)/2 = -12], verify avgRMS calculation
7. `takes max peak across channels` - Mock [-5, -8] peak, verify maxPeak = -5
8. `stops monitoring on error` - Mock client that throws, verify stopInputLevelMonitoring called in finally
9. `handles validation error for invalid target_rms` - target_rms=10 (invalid), verify ZodError handling
  </action>
  <verify>`npm test -- --testPathPattern="api-check-levels" --run` passes all tests</verify>
  <done>api-check-levels.ts has comprehensive test coverage</done>
</task>

<task type="auto">
  <name>Task 3: Create api-calibrate-spl.test.ts</name>
  <files>src/tools/api-calibrate-spl.test.ts</files>
  <action>
Create new test file following patterns from api-generator.test.ts.

Use vi.mock for getActiveApiClient and vi.useFakeTimers for setTimeout delays.

Test cases for start action:
1. `configures generator with pink noise` - Verify setGeneratorSignal called with 'Pink noise'
2. `sets generator level to -20 dBFS` - Verify setGeneratorLevel called with -20
3. `starts generator and waits for stabilization` - Verify 2000ms delay (vi.advanceTimersByTime)
4. `configures SPL meter with weighting` - Verify setSPLMeterConfig called with weighting='C'
5. `starts SPL meter and waits for averaging` - Verify 1000ms delay
6. `returns connection error when not connected` - getActiveApiClient returns null

Test cases for check action:
1. `reads SPL meter and returns current level` - Mock levels, verify current_spl
2. `calculates adjustment needed` - target=85, current=82, verify adjustment_needed=3
3. `returns within_tolerance true when in range` - 84.5 dB for 85+/-1 target, verify within_tolerance=true
4. `returns within_tolerance false when out of range` - 82 dB for 85+/-1 target, verify within_tolerance=false
5. `provides increase guidance when low` - 82 dB target 85, verify guidance contains "Increase"
6. `provides decrease guidance when high` - 88 dB target 85, verify guidance contains "Decrease"
7. `returns should_block_measurement true when not calibrated` - verify should_block_measurement=!within_tolerance
8. `returns error when SPL meter read fails` - Mock null levels, verify error response

Test cases for stop action:
1. `stops generator` - Verify executeGeneratorCommand('Stop') called
2. `stops SPL meter` - Verify executeSPLMeterCommand(meterId, 'Stop') called
3. `returns success message` - Verify message indicates stopped

General:
1. `handles validation error for invalid action` - Invalid action, verify ZodError
2. `handles REWApiError` - Mock client that throws REWApiError, verify error response
  </action>
  <verify>`npm test -- --testPathPattern="api-calibrate-spl" --run` passes all tests</verify>
  <done>api-calibrate-spl.ts has comprehensive test coverage</done>
</task>

</tasks>

<verification>
1. `npm test -- --run` - All tests pass
2. `npm run test:coverage` - Coverage maintained at 80%+ for API, 70%+ for tools
3. New tool tests cover all branches (zone determination, actions, error cases)
</verification>

<success_criteria>
- All new tests pass
- Test coverage does not regress below Phase 2 thresholds
- Zone boundary tests verify exact threshold behavior (-3, -10, -20, -40 dBFS)
- SPL calibration tests verify stabilization delays and tolerance calculation
- Error handling tests cover all error paths (connection, validation, API errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-calibration-setup-tools/03-04-SUMMARY.md`
</output>

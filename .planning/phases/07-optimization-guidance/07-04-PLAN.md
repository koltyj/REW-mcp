---
phase: 07-optimization-guidance
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - src/optimization/recommendations.test.ts
  - src/optimization/validation.test.ts
  - src/optimization/success-criteria.test.ts
  - src/tools/optimize-room.test.ts
autonomous: true

must_haves:
  truths:
    - "Recommendation generation is tested for all element types"
    - "Validation thresholds are tested at boundaries"
    - "Success criteria zones are tested at exact thresholds"
    - "MCP tool actions are tested with mocked data"
  artifacts:
    - path: "src/optimization/recommendations.test.ts"
      provides: "Recommendation tests"
      min_lines: 80
    - path: "src/optimization/validation.test.ts"
      provides: "Validation tests"
      min_lines: 100
    - path: "src/optimization/success-criteria.test.ts"
      provides: "Success criteria tests"
      min_lines: 80
    - path: "src/tools/optimize-room.test.ts"
      provides: "Tool tests"
      min_lines: 100
  key_links:
    - from: "src/optimization/recommendations.test.ts"
      to: "src/optimization/recommendations.ts"
      via: "test imports"
      pattern: "import.*from.*recommendations"
    - from: "src/optimization/validation.test.ts"
      to: "src/optimization/validation.ts"
      via: "test imports"
      pattern: "import.*validateAdjustment"
---

<objective>
Create comprehensive unit tests for Phase 7 optimization modules

Purpose: Verify recommendation generation, validation thresholds, and success criteria zones work correctly at all boundary conditions. Test MCP tool actions with realistic scenarios.
Output: Test files for all optimization modules with >70% coverage
</objective>

<execution_context>
@/Users/koltonjacobs/.claude-gsd/get-shit-done/workflows/execute-plan.md
@/Users/koltonjacobs/.claude-gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-optimization-guidance/07-CONTEXT.md
@.planning/phases/07-optimization-guidance/07-RESEARCH.md
@.planning/phases/07-optimization-guidance/07-01-SUMMARY.md
@.planning/phases/07-optimization-guidance/07-02-SUMMARY.md
@.planning/phases/07-optimization-guidance/07-03-SUMMARY.md
@src/interpretation/index.test.ts
@src/tools/api-measurement-session.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recommendation and validation tests</name>
  <files>src/optimization/recommendations.test.ts, src/optimization/validation.test.ts</files>
  <action>
Create `src/optimization/recommendations.test.ts`:

1. **generatePlacementRecommendation tests**:
   - SBIR issue with Q>10 returns 'high' confidence
   - SBIR issue with Q=6 returns 'medium' confidence
   - Room mode issue with dimensions returns monitor/listening position guidance
   - Null issue recommends repositioning (not EQ)
   - Result includes physics context in reason field
   - Result includes expected_improvement estimate

2. **generateSubRecommendation tests**:
   - Sub issue includes phase_suggestion when phase inversion data provided
   - Sub issue includes boundary_loading context ("Corner adds +3dB per boundary")
   - Sub issue near 80Hz includes crossover_context
   - More detailed than generic placement recommendation

3. **generateListeningPositionRecommendation tests**:
   - With room dimensions: Uses 38% rule ("Move to ~X ft from front wall")
   - Without dimensions: Generic "try moving forward/backward"
   - For null at calculated mode position: Suggests avoiding null zone

4. **determineElement tests**:
   - SBIR -> 'monitors'
   - sub_integration -> 'subwoofer'
   - lr_symmetry -> 'monitors'
   - room_modes < 80Hz -> 'subwoofer' or 'listening_position'

Create `src/optimization/validation.test.ts`:

1. **validateAdjustment classification tests**:
   - 50% improvement -> 'success'
   - 49% improvement -> 'partial'
   - 25% improvement -> 'partial'
   - <1 dB change on small issue (<6 dB) -> 'unchanged'
   - <2 dB change on medium issue (6-10 dB) -> 'unchanged'
   - <3 dB change on large issue (>10 dB) -> 'unchanged'
   - -11% change -> 'worsened'
   - -9% change -> 'unchanged' (within noise threshold)

2. **next_action guidance tests**:
   - 'success' includes "Good improvement" message
   - 'worsened' includes "Try moving opposite direction"
   - 'unchanged' suggests "different adjustment"
   - 'partial' mentions "fine-tuning" option

3. **summary format tests**:
   - Includes both dB value and percentage
   - Example: "Variance improved from 8.2dB to 4.1dB (50% reduction)"

Use vi.mock for measurementStore if needed. Create mock StoredMeasurement objects with realistic frequency_response data.
  </action>
  <verify>npm test -- recommendations.test.ts validation.test.ts passes</verify>
  <done>Recommendation and validation tests cover key scenarios and thresholds</done>
</task>

<task type="auto">
  <name>Task 2: Create success criteria and tool tests</name>
  <files>src/optimization/success-criteria.test.ts, src/tools/optimize-room.test.ts</files>
  <action>
Create `src/optimization/success-criteria.test.ts`:

1. **Zone threshold tests** (test at exact boundaries):
   - Smoothness 3.0 dB -> 'good'
   - Smoothness 3.1 dB -> 'acceptable'
   - Smoothness 5.0 dB -> 'acceptable'
   - Smoothness 5.1 dB -> 'needs_work'
   - L/R balance 1.0 dB -> 'good'
   - L/R balance 1.1 dB -> 'acceptable'
   - L/R balance 2.0 dB -> 'acceptable'
   - L/R balance 2.1 dB -> 'needs_work'
   - Sub integration 4.0 dB -> 'good'
   - Sub integration 6.0 dB -> 'acceptable'
   - Sub integration 6.1 dB -> 'needs_work'

2. **overall_zone tests**:
   - All 'good' -> overall 'good'
   - One 'needs_work' among 'good' -> overall 'needs_work'
   - Mix of 'acceptable' and 'good' -> overall 'acceptable'

3. **should_stop tests**:
   - smoothness 'good' -> should_stop = true
   - smoothness 'acceptable' -> should_stop = false
   - smoothness 'good' but L/R 'needs_work' -> should_stop = true (per CONTEXT.md, smoothness is primary)

4. **Message format tests**:
   - 'good' zone includes "further gains will be marginal"
   - 'needs_work' zone includes variance value and target

Create `src/tools/optimize-room.test.ts`:

1. **get_recommendation action tests**:
   - Returns single recommendation (not array)
   - Includes next_steps guidance
   - Returns error for missing measurement_id

2. **validate_adjustment action tests**:
   - Requires pre_measurement_id, returns error if missing
   - Returns improvement_type and next_action
   - Worsened case includes "opposite direction" guidance

3. **check_progress action tests**:
   - Returns all three zone evaluations
   - should_stop reflects smoothness zone
   - progress_summary is plain language

4. **Error handling tests**:
   - measurement_not_found returns helpful message
   - Invalid action returns validation error

Use vi.mock for measurementStore.get to return controlled test data. Create helper function to generate mock StoredMeasurement with configurable variance.
  </action>
  <verify>npm test -- success-criteria.test.ts optimize-room.test.ts passes</verify>
  <done>All optimization tests pass with proper threshold coverage</done>
</task>

</tasks>

<verification>
1. All tests pass: `npm test`
2. Coverage check: `npm run test:coverage` shows optimization module > 70%
3. Threshold tests at exact boundaries (3.0, 3.1 for smoothness zones)
4. No skipped or pending tests
</verification>

<success_criteria>
- src/optimization/recommendations.test.ts tests all element types and confidence levels
- src/optimization/validation.test.ts tests 50%/unchanged/worsened thresholds
- src/optimization/success-criteria.test.ts tests zone boundaries at exact values
- src/tools/optimize-room.test.ts tests all three actions
- All tests pass with `npm test`
- Coverage exceeds 70% for optimization module
</success_criteria>

<output>
After completion, create `.planning/phases/07-optimization-guidance/07-04-SUMMARY.md`
</output>

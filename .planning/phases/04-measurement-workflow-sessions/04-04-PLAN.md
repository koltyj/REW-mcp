---
phase: 04-measurement-workflow-sessions
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - src/session/session-state.test.ts
  - src/session/sequence-state-machine.test.ts
  - src/tools/api-measurement-session.test.ts
autonomous: true

must_haves:
  truths:
    - "Session state functions are tested for create/get/update/list/end"
    - "State machine transitions are tested for valid and invalid paths"
    - "Tool actions are tested with mocked API client"
    - "Pro license 403 error is tested"
    - "Concurrent sessions are tested for isolation"
  artifacts:
    - path: "src/session/session-state.test.ts"
      provides: "Unit tests for session state module"
      contains: "describe.*session-state"
    - path: "src/session/sequence-state-machine.test.ts"
      provides: "Unit tests for state machine"
      contains: "describe.*sequence-state-machine"
    - path: "src/tools/api-measurement-session.test.ts"
      provides: "Unit tests for measurement session tool"
      contains: "describe.*api-measurement-session"
  key_links:
    - from: "src/tools/api-measurement-session.test.ts"
      to: "../session/session-state.ts"
      via: "import and clear sessions between tests"
      pattern: "activeSessions|beforeEach"
---

<objective>
Create comprehensive unit tests for Phase 4 session and tool modules.

Purpose: Validate session state management, state machine transitions, and tool behavior with edge cases.
Output: Test files with >80% coverage for Phase 4 modules.
</objective>

<execution_context>
@/Users/koltonjacobs/.claude-gsd/get-shit-done/workflows/execute-plan.md
@/Users/koltonjacobs/.claude-gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-measurement-workflow-sessions/04-03-SUMMARY.md

Reference test patterns:
- src/tools/api-calibrate-spl.test.ts (Phase 3 test patterns)
- src/tools/api-measure.test.ts (vi.mock for getActiveApiClient)
- Decision 02-04: Use vi.mock for getActiveApiClient instead of MSW
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create session state module tests</name>
  <files>src/session/session-state.test.ts</files>
  <action>
Create unit tests for session state management:

1. Import dependencies:
   - import { describe, it, expect, beforeEach } from 'vitest'
   - Import all functions from session-state.ts
   - Need to export activeSessions Map for test cleanup (or add clearAllSessions for testing)

2. Test cases for createSession:
   - Creates session with unique UUID
   - Sets sequence_step to 'idle'
   - Sets created_at to current timestamp
   - Stores notes when provided
   - Session is stored in activeSessions

3. Test cases for getSession:
   - Returns session when exists
   - Throws error with suggestion when not found
   - Error message includes session ID

4. Test cases for updateSession:
   - Updates existing session fields
   - Does not mutate original object (clone)
   - Throws when session not found
   - Partial updates preserve existing fields

5. Test cases for listActiveSessions:
   - Returns empty array when no sessions
   - Returns all active sessions
   - Sorts by created_at descending (most recent first)

6. Test cases for endSession:
   - Removes session from activeSessions
   - No-op when session doesn't exist (doesn't throw)

7. Test concurrent sessions:
   - Create multiple sessions
   - Each has unique ID
   - Updating one doesn't affect others
   - Listing returns all

Add beforeEach to clear activeSessions between tests.
Use vi.useFakeTimers() for timestamp-dependent tests if needed.
  </action>
  <verify>`npm test src/session/session-state.test.ts` passes</verify>
  <done>Session state module has comprehensive test coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create state machine tests</name>
  <files>src/session/sequence-state-machine.test.ts</files>
  <action>
Create unit tests for state machine:

1. Import dependencies:
   - import { describe, it, expect } from 'vitest'
   - Import all functions from sequence-state-machine.ts

2. Test cases for validateTransition:
   - Valid: idle -> measuring_left (passes)
   - Valid: measuring_left -> measuring_right (passes)
   - Valid: measuring_right -> measuring_sub (passes)
   - Valid: measuring_sub -> complete (passes)
   - Invalid: idle -> measuring_right (throws)
   - Invalid: idle -> measuring_sub (throws)
   - Invalid: measuring_left -> measuring_sub (throws, skipping right)
   - Invalid: complete -> anything (throws)
   - Error message includes from, to, and expected steps

3. Test cases for getNextStep:
   - idle -> 'measuring_left'
   - measuring_left -> 'measuring_right'
   - measuring_right -> 'measuring_sub'
   - measuring_sub -> 'complete'
   - complete -> null

4. Test cases for getStepGuidance:
   - Each step returns non-empty string
   - Guidance mentions expected action (e.g., "Measure Left" for idle)

5. Test cases for channelToStep:
   - 'left' -> 'measuring_left'
   - 'right' -> 'measuring_right'
   - 'sub' -> 'measuring_sub'
  </action>
  <verify>`npm test src/session/sequence-state-machine.test.ts` passes</verify>
  <done>State machine has comprehensive test coverage</done>
</task>

<task type="auto">
  <name>Task 3: Create measurement session tool tests</name>
  <files>src/tools/api-measurement-session.test.ts</files>
  <action>
Create unit tests for measurement session tool:

1. Import dependencies:
   - import { describe, it, expect, beforeEach, vi } from 'vitest'
   - Import executeApiMeasurementSession
   - vi.mock('./api-connect.js') for getActiveApiClient
   - Import session functions for cleanup

2. Mock setup:
   - Mock getActiveApiClient to return mock REWApiClient
   - Mock client methods: setMeasureNotes, setBlockingMode, listMeasurements, executeMeasureCommand
   - Clear sessions between tests

3. Test start_session action:
   - Creates session and returns session_id
   - Returns guidance for next step
   - Rejects if session_id provided

4. Test measure action:
   - Requires session_id (error if missing)
   - Requires channel (error if missing)
   - Session not found returns session_error with suggestion
   - Invalid sequence transition returns error
   - Sets measurement name before measuring
   - Enables blocking mode
   - Executes Measure command
   - Updates session state on success
   - Returns measurement details and guidance
   - 403 status returns license_error with upgrade URL

5. Test get_status action:
   - With session_id: returns session details and guidance
   - Without session_id: lists all active sessions
   - Session not found returns error with suggestion

6. Test stop_session action:
   - Requires session_id (error if missing)
   - Ends session and returns summary
   - Session not found returns error

7. Test error handling:
   - Not connected returns connection_error
   - Zod validation errors return validation_error
   - REWApiError mapped to appropriate type

8. Test full workflow:
   - start_session -> measure(left) -> measure(right) -> measure(sub) -> stop_session
   - Verify state transitions at each step

Use vi.useFakeTimers() for timestamp testing if needed.
Use vi.advanceTimersByTimeAsync for any setTimeout operations.
  </action>
  <verify>`npm test src/tools/api-measurement-session.test.ts` passes</verify>
  <done>Measurement session tool has comprehensive test coverage</done>
</task>

</tasks>

<verification>
1. All tests pass: `npm test src/session/ src/tools/api-measurement-session.test.ts`
2. Coverage meets targets: `npm run coverage` shows >80% for session modules
3. Full workflow test validates end-to-end sequence
</verification>

<success_criteria>
- Session state module: all CRUD operations tested
- State machine: valid and invalid transitions tested
- Tool: all 4 actions tested with success and error cases
- Pro license 403 error tested with correct message
- Concurrent session isolation tested
- Full L/R/Sub workflow tested end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-measurement-workflow-sessions/04-04-SUMMARY.md`
</output>
